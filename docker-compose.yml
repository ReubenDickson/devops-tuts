version: "3.8"

services:
  web:
    image: nginx
    ports:
      - "8080:80"
    volumes:
      - ./html:/usr/share/nginx/html
    environment:
      - APP_ENV=production

  reddis:
    image: redis
    volumes:
     - redis_data:/data

volumes:
  redis_data:
